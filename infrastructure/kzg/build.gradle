dependencies {
  implementation project(':infrastructure:io')
  implementation project(':infrastructure:http')

  implementation 'org.apache.tuweni:tuweni-bytes'
  implementation 'org.apache.tuweni:tuweni-ssz'
  implementation 'tech.pegasys:jc-kzg-4844'
  implementation "io.github.crate-crypto:peerdas-kzg"
  implementation 'commons-io:commons-io'

  testFixturesImplementation 'com.google.guava:guava'
}
// This is temporary and allows us to test with the rust kzg lib
// TODO: we likely want this for building too
task testWithRustKZG(type: Test) {
    systemProperty 'use.rust.kzg', 'true'
}